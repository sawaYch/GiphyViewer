import Layout from './Layout';
import TabNavigator from './TabNavigator';

const GiphyViewer = () => {
  return (
    <Layout>
      <TabNavigator />
    </Layout>
  );
};

export default GiphyViewer;
